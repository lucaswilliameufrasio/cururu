# Cururu docs site

Astro static site for Cururu product introduction and integration guidance.

```sh
pnpm install
pnpm dev
pnpm build
```

The self-hosted GitHub App guide describes the supported `cururu serve` mode and its PostgreSQL/SQLite deployment options.
